# {{projectName}}

Next.js app (Verno `next-app`). npm scope: `@{{npmScope}}`.

If you skipped steps during `verno create`, run `shadcn` and `ultracite` in this directory.

To apply a different shadcn preset on an existing project: `<package runner> shadcn@latest apply --preset <code>` (see [shadcn CLI](https://ui.shadcn.com/docs/cli)).
